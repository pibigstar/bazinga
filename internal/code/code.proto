syntax = "proto3";

option go_package = "internal/code;code";

// 公共Code错误码
enum Error {
    Unknown        = 0;    // 未知错误
    Success        = 200;  // 操作成功
    Internal       = 500;  // 服务器内部错误
    RecordNotFount = 600;  // 服务器内部错误
}